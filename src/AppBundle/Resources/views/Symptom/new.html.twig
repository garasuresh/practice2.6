{% extends 'AppBundle::layout.html.twig' %}

{% block content %}
    <h1>Symptom creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('symptom') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
